#include <stdio.h>
#include "main.h"

/**
* _abs -  function
* @n: number to check
*
* Return: No return in this function
*/
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
