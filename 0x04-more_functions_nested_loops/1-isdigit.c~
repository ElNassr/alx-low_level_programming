#include <stdio.h>
#include "main.h"

/**
 * _isupper -  function
 * @c: char input to chack
 *
 * Return: 1 if lower, 0 otherwise
 */
int _isupper(int c)
{
	int i = 65;
	int j = 90;

	if (c >= i)
	{
		if (c <= j)
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
	else
	{
		return (0);
	}
}
